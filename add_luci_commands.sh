#!/bin/bash
#
# TODO: A utility to add some neat commands to LuCI's custom commands page.
#
# This is where it's done. Can examine them, and look for them and if not there add them.
# Any commands we add to LuCI need to go into /usr/bin along with dependencies.
uci show luci